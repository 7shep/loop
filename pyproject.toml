[build-system]
requires = []
build-backend = "build_backend"
backend-path = ["."]

[project]
name = "loop-assignment-orchestrator"
version = "0.1.0"
description = "A review-gated, evidence-backed assignment workflow for Codex conversations."
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "Alex" }]
dependencies = []

[project.scripts]
loop = "loop.cli:main"
